import LogicFlow from "@logicflow/core/src/LogicFlow";

export const DefaultTheme = {
    nodeText: {
        overflowMode: 'autoWrap',
    },
    edgeText: {
        overflowMode: 'autoWrap'
    }
} as LogicFlow.Theme;
